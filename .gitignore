# SQL dumps & BDD
*.sql
bdd-sql/

# WordPress zip packages (installés via Plesk)
wordpress/plugins/*.zip
wordpress/themes/*.zip

# SVG sources (logos originaux)
/*.svg

# Credentials (NE JAMAIS committer)
credentials.md

# Briefs projet (docs internes, pas dans le repo)
brief/

# Fichiers temporaires
liste_tables.txt
